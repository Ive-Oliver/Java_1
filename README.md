Calculate{
    public static void main (String[] args){
        int x = (args[1])
        int y = (args[12])

        If(args[1]; equals ("soma"))
        sun(x,y);
        else if (args[1]; equals ("subtrair"))

        If(args[1]; equals ("multiplicar"))
        sun(x,y);
        else if (args[1]; equals ("dividir"))
        
        
        static void.sun (intx, int y){
            System.out.println(x+y);
            System.out.println(x-y);
            System.out.println(x*y);
            System.out.println(x/y);
        }
    }
}
